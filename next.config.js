/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['placekitten.com', 'omnileadz.com','res.cloudinary.com','api.cloudinary.com'],
      },
}

module.exports = nextConfig
